using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum ReturnValue
{
    Default = 0,
    WasStarted = 10,
    WasStopped = 20,
    Succeeded = 30,
    Failed = 40,
}
